---
- hosts: vyos_vclos_leafs
  tasks:
    - name: Test Reachability on Leafs!
      ping:
- hosts: vyos_vclos_spines
  tasks:
    - name: Test Reachability on Spines!
      ping:
- hosts: vclos_s0.engyak.net
  tasks:
    - name: Test Reachability on S0!
      ping:
